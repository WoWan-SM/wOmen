# Trading Agent - Автоматизированная торговая система

Автоматизированная торговая система для работы с Tinkoff Invest API. Система включает полное логирование всех действий в базу данных, управление состояниями, динамический риск-менеджмент и уведомления в Telegram.

## 🚀 Основные возможности

- ✅ **Исправлены критические ошибки**: NaN в индикаторах, валидация данных, управление лотностью
- ✅ **Полное логирование в БД**: Все решения и действия записываются в MariaDB
- ✅ **State Machine**: Управление состояниями торговли (SCANNING, ENTRY_PENDING, ACTIVE, EXIT_PENDING, COOLDOWN)
- ✅ **Динамический риск-менеджмент**: ATR-based стопы с трейлинг-стопом до безубытка
- ✅ **Фильтры рынка**: ADX < 25 (флет), спред > 0.05%
- ✅ **Telegram уведомления**: О покупках, ошибках, закрытии позиций, ежедневные отчеты
- ✅ **REST API для статистики**: Анализ решений и результатов торговли
- ✅ **Backtesting Engine**: Тестирование стратегии на исторических данных без реальных сделок
- ✅ **Dashboard API**: REST endpoints для визуализации данных (совместимо с Grafana)

## 📋 Требования

- Java 21+
- MariaDB 10.5+
- Tinkoff Invest API токен (Sandbox или Production)
- Telegram Bot Token (опционально, для уведомлений)

## ⚙️ Настройка

### 1. База данных MariaDB

Создайте базу данных:
```sql
CREATE DATABASE trading_agent CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

Настройте подключение в `src/main/resources/application.properties`:
```properties
spring.datasource.url=jdbc:mariadb://localhost:3306/trading_agent?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=ваш_пароль
```

### 2. Tinkoff API

Настройте токен Tinkoff API в коде (см. `TinkoffApi.java`).

### 3. Telegram Bot (опционально)

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Получите Chat ID:
   - Отправьте сообщение боту
   - Используйте: `https://api.telegram.org/bot<TOKEN>/getUpdates`
   - Найдите `chat.id` в ответе

Добавьте в `application.properties`:
```properties
telegram.bot.token=ваш_токен_бота
telegram.chat.id=ваш_chat_id
```

## 🏃 Запуск

```bash
./gradlew bootRun
```

Или через IDE: запустите `TradingAgentApplication`.

## 📊 API Endpoints

### Торговля
- `GET /api/scan-market` - Сканирование рынка и анализ индикаторов
- `POST /api/execute-trade` - Выполнение сделки (вызывается из n8n)

### Статистика
- `GET /api/statistics/decisions?from=2024-01-01T00:00:00Z&to=2024-01-31T23:59:59Z` - Статистика решений
- `GET /api/statistics/positions?from=2024-01-01T00:00:00Z&to=2024-01-31T23:59:59Z` - Статистика позиций (PnL)
- `GET /api/statistics/decisions/latest?limit=50` - Последние решения
- `GET /api/statistics/daily?date=2024-01-20` - Дневная статистика

### Backtesting
- `GET /api/backtest/run?ticker=SBER&days=30&interval=HOUR` - Запуск бэктеста стратегии на исторических данных
  - Параметры:
    - `ticker` - Тикер инструмента (обязательно)
    - `days` - Количество дней для бэктеста (по умолчанию 30)
    - `interval` - Интервал свечей: HOUR, DAY, 15_MIN (по умолчанию HOUR)
  - Возвращает: PnL, количество сделок, win rate, максимальную просадку и список всех сделок

### Dashboard
- `GET /api/dashboard/overview?hours=24` - Общая статистика для dashboard
- `GET /api/dashboard/balance-chart?days=7` - Данные для графика баланса (PnL по дням)
- `GET /api/dashboard/open-positions` - Список открытых позиций

### Отчеты
- `GET /api/reports/excel` - Excel отчет по сделкам

## 🗄️ Структура базы данных

### market_snapshot
Снимки рынка при каждом анализе тикера:
- timestamp, ticker, price_close
- rsi_value, adx_value, macd_value, atr_value, ema_value

### trade_decision
Логи принятия решений (ВАЖНО: даже если сделка не открылась):
- snapshot_id (FK), decision (BUY/SELL/HOLD/IGNORE)
- reason_code, reason_details

### orders
Учет ордеров:
- broker_order_id, ticker, direction, requested_price, executed_price
- quantity_lots, commission, status (NEW/FILLED/REJECTED), error_message

### positions
Учет позиций для PnL:
- entry_order_id, exit_order_id, entry_price, exit_price
- pnl_absolute, pnl_percent, duration_seconds

## 🔔 Telegram уведомления

Система отправляет уведомления:
- ✅ О покупке/продаже: "Купил SBER по 250.00 (лотов: 1)"
- ❌ Об ошибках: "ERROR: Недостаточно средств"
- ⛔ Об отказах: "ОТКАЗ: SBER - ADX < 25"
- 📈 Об открытии позиции: "ПОЗИЦИЯ ОТКРЫТА: SBER, Вход: 250.00, SL: 245.00, TP: 265.00"
- 💰 О закрытии позиции: "ПОЗИЦИЯ ЗАКРЫТА: SBER, PnL: +50.00 руб (2.00%)"
- 📊 Ежедневные отчеты в 23:00

## 🎯 State Machine

Система управляет состояниями для каждого тикера:
- **SCANNING** - Ищем точку входа
- **ENTRY_PENDING** - Ордер отправлен, ждем исполнения
- **ACTIVE** - В позиции, мониторим SL/TP
- **EXIT_PENDING** - Ордер на выход отправлен
- **COOLDOWN** - После сделки не торгуем 60 минут

## 📈 Риск-менеджмент

- **ATR-based SL/TP**: Stop Loss = EntryPrice - (ATR * 2), Take Profit = EntryPrice + (ATR * 3)
- **Trailing Stop**: Если цена ушла в плюс на 1 * ATR, стоп переносится в безубыток
- **Проверка баланса**: Перед каждым ордером проверяется наличие средств
- **Фильтр спреда**: Если спред > 0.05%, сделка отменяется

## 🛠️ Разработка

### Структура проекта
```
src/main/java/com/example/tradingagent/
├── controllers/          # REST API контроллеры
├── services/             # Бизнес-логика
├── entities/             # JPA сущности
├── repositories/         # JPA репозитории
└── dto/                  # Data Transfer Objects
```

### Основные сервисы
- `TinkoffMarketDataService` - Получение рыночных данных
- `TechnicalIndicatorService` - Расчет индикаторов (RSI, ADX, MACD, ATR, EMA)
- `TinkoffOrderService` - Исполнение ордеров
- `AuditService` - Логирование в БД
- `TradingStateMachine` - Управление состояниями
- `TelegramNotificationService` - Уведомления в Telegram
- `PositionMonitoringService` - Мониторинг закрытых позиций
- `DailyReportService` - Ежедневные отчеты

## 📝 Roadmap выполнен

✅ **Этап 1**: Исправление критических ошибок (NaN, валидация, лотность, баланс, увеличение количества свечей N*3)  
✅ **Этап 2**: Логирование в БД (MarketSnapshot, TradeDecision, Orders, Positions)  
✅ **Этап 3**: Рефакторинг (State Machine, ATR риск-менеджмент, фильтры рынка)  
✅ **Этап 4**: Развитие (Telegram уведомления, мониторинг позиций, REST API статистики, **Backtesting Engine**, **Dashboard API**)

## ⚠️ Важные замечания

1. **Sandbox режим**: По умолчанию используется Sandbox счет. Для production измените настройки.
2. **Комиссия**: Убедитесь, что `ESTIMATED_COMMISSION_RATE` соответствует вашему тарифу (0.05% для Трейдера, 0.3% для Инвестора).
3. **База данных**: Таблицы создаются автоматически при первом запуске (`spring.jpa.hibernate.ddl-auto=update`).
4. **Telegram**: Уведомления работают только если настроены `telegram.bot.token` и `telegram.chat.id`.

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи приложения
2. Таблицы в БД (market_snapshot, trade_decision)
3. Настройки Telegram (если используются уведомления)
4. Подключение к Tinkoff API

---

**Версия**: 0.0.1-SNAPSHOT  
**Статус**: Pre-Alpha / Development
